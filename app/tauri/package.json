{
  "name": "ciphernet",
  "version": "1.0.0",
  "description": "CIPHER//NET — end-to-end encrypted decentralized chat",
  "scripts": {
    "build:web": "node sync-web.js",
    "dev":   "node sync-web.js && tauri dev",
    "build": "node sync-web.js && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  }
}